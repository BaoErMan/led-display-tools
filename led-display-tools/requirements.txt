# led-display-tools dependencies
#
# Only ONE Python package is needed, and only by the tools that talk to a wall:
pyserial

# Not pip-installable, but needed by some tools:
#   tkinter  - led_ambient_gui.py only. Ships with Python on Windows;
#              on Debian/Ubuntu: sudo apt install python3-tk
#   tshark   - the offline capture analysers only (Wireshark CLI).
#              Windows: install Wireshark. Debian/Ubuntu: sudo apt install tshark
#
# The pure-offline tools (hd_auto_compare.py, hd_template_check.py --against,
# led_vendor.py, analyze.py) need nothing but the standard library.
